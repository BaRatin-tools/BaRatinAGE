package bam.utils;

import java.util.ArrayList;
// import java.util.List;
// import java.util.AbstractMap.SimpleEntry;
import java.util.Map.Entry;

public class NamedArrayList<K, V> extends ArrayList<Entry<K, V>> {

    // NamedArrayList(List<K> headers, List<V> values) {
    // if (headers.size() != values.size()) {
    // throw new IllegalArgumentException("Headers and values must hve the same
    // size!");
    // }
    // for (int k = 0; k < headers.size(); k++) {
    // this.add(new SimpleEntry<K, V>(headers.get(k), values.get(k)));
    // }
    // }

}
